# Changesets

This directory holds pending changesets — see the
[Changesets documentation](https://github.com/changesets/changesets) for
instructions on how to add one (`pnpm changeset`) and how versioning/publish
via GitHub Actions works for this package.
